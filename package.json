{
  "name": "portal-energi",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:css": "npx tailwindcss -i tailwind-input.css -o tailwind-output.css --minify",
    "build:css:watch": "npx tailwindcss -i tailwind-input.css -o tailwind-output.css --watch",
    "dev": "npm run build:css:watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
